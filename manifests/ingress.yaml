apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: troubleshooting-ingress
  namespace: "82265604"
spec:
  ingressClassName: ingress-internal
  rules:
  # 사번으로 변경하세요
  - host: 82265604a.ce-aa.kubepia.net
    http:
      paths:
      - path: /
        backend:
          service:
            name: troubleshooting
            port:
              number: 8080
        pathType: Prefix
